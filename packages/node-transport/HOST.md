# Deprecated trusted-host name

The Cube product now calls this a **trusted runner**. See [RUNNER.md](RUNNER.md)
and the [operator runbook](../../docs/trusted-runner-operations.md).

Compatibility remains for `host-init`, `host-serve`, `/host-exec`, and the
`host` wire profile during the documented migration window. Do not use these
names in new automation.
